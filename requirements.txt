# Dependencies for the Letter-Counting GRPO + LoRA notebook.
# Target: Linux + NVIDIA GPU (>= 16 GB VRAM, CUDA 12.x) — e.g. the
# Udacity / Vocareum T4 workspace.
#
# Install from the workspace terminal with:
#   pip install -r requirements.txt
# or, if the workspace uses uv:
#   uv pip install -r requirements.txt
#
# Note: the Vocareum workspace usually ships these pre-installed. Only run
# the install if `import unsloth` fails in the notebook.

unsloth>=2025.1
vllm>=0.6
torch>=2.4
transformers>=4.45,<6
trl>=0.21
peft>=0.13
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.43
matplotlib>=3.8
ipykernel>=6.29
